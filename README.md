# Gartic Beep

This is the repository for Gartic Beep, a game similar to telephone or gartic phone but done with beepbox links. 

You can test by running `npm run dev` in both the backend and frontend folders, build with `./build.sh`, and test the build with `./test.sh`. 
