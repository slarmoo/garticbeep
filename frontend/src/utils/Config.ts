export interface DiscordData {
    username: string,
    discriminator: string,
    avatar: string,
    id: string
}

export interface roundData {
    number: number,
    type: string,
    utc: number
}